[project]
name = "godin-consumer"
version = "0.1.0"
description = "Ejemplo mínimo de sistema consumidor de Minerva (OIDC + minerva_sdk)"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.110",
    "uvicorn>=0.29",
    "httpx>=0.27",
    "minerva-sdk @ file://../../sdk",
]
