# Sin MINERVA_CLIENT_SECRET: este ejemplo registra "Godín" como cliente público
# (is_public=true), así que el canje de código se hace solo con PKCE.
MINERVA_ISSUER_URL=http://localhost:9000
MINERVA_APPLICATION_CODE=godin
MINERVA_CLIENT_ID=
MINERVA_REDIRECT_URI=http://localhost:8100/callback
